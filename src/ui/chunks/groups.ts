/**
 * Starting a group and looking inside one, as a lazy chunk. A conversation
 * itself — group or not — is the shell's ChatView. See `src/ui/lazyViews.tsx`.
 */
export { NewGroup } from '../groups/NewGroup'
export { GroupInfo } from '../groups/GroupInfo'
